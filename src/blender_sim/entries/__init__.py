"""
Blender CLI entry scripts — run only inside Blender (`blender --background --python ...`).

Each file reads a job JSON path from argv and dispatches to implementation modules in `blender_sim`.
"""
