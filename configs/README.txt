configs/ 안 설정 파일 역할

1) default_config.yaml
   - 전역 파이프라인 (main.py 등) + Blender 실행 파일 경로.
   - blender.assets_root 기본: data/Tangerine_3D

귤 3D 전용 YAML(base_mesh, variants_batch)은 **data/Tangerine_3D/configs/** 에 있습니다.
